import { KeystoreGenerator } from "@/components/KeystoreGenerator";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col">
      <KeystoreGenerator />
    </main>
  );
}
